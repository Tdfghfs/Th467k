# المساهمة في مشروع ايـزن

شكراً لاهتمامك بالمساهمة في مشروع ايـزن! نرحب بجميع المساهمات سواء كانت تحسينات أو تصحيحات أخطاء أو ميزات جديدة.

## كيفية المساهمة

### 1. انسخ المستودع (Fork)

انقر على زر Fork في الزاوية العلوية اليمنى من صفحة المستودع.

### 2. استنسخ نسختك المحلية

```bash
git clone https://github.com/yourusername/aizen-ai.git
cd aizen-ai
```

### 3. أنشئ فرع جديد

```bash
git checkout -b feature/your-feature-name
# أو للإصلاحات
git checkout -b fix/your-bug-fix
```

### 4. قم بالتغييرات

- اتبع معايير الكود الموجودة
- اكتب اختبارات للميزات الجديدة
- تأكد من أن جميع الاختبارات تمر

### 5. اختبر التغييرات

```bash
pnpm test
pnpm build
```

### 6. اجعل التغييرات (Commit)

```bash
git add .
git commit -m "feat: وصف واضح للتغيير"
```

استخدم الرسائل الواضحة والموجزة:
- `feat:` للميزات الجديدة
- `fix:` لإصلاح الأخطاء
- `docs:` لتحديثات التوثيق
- `style:` لتغييرات التنسيق
- `refactor:` لإعادة هيكلة الكود

### 7. ادفع التغييرات

```bash
git push origin feature/your-feature-name
```

### 8. افتح طلب دمج (Pull Request)

1. اذهب إلى صفحة المستودع الأصلي
2. انقر على "Pull Requests"
3. انقر على "New Pull Request"
4. اختر فرعك وأكمل النموذج

## معايير الكود

- استخدم TypeScript
- اتبع معايير ESLint
- اكتب تعليقات واضحة للكود المعقد
- استخدم أسماء متغيرات واضحة

## الاختبارات

جميع الميزات الجديدة يجب أن تأتي مع اختبارات:

```bash
# تشغيل الاختبارات
pnpm test

# تشغيل اختبار معين
pnpm test server/your-feature.test.ts
```

## التوثيق

- حدّث README.md إذا أضفت ميزة جديدة
- اكتب تعليقات واضحة في الكود
- وثّق أي متغيرات بيئة جديدة

## الإبلاغ عن الأخطاء

عند الإبلاغ عن خطأ:

1. تحقق من أنه لم يتم الإبلاغ عنه مسبقاً
2. اشرح الخطأ بوضوح
3. قدم خطوات لإعادة إنتاج الخطأ
4. أرفق لقطات شاشة إن أمكن

## طلب ميزات جديدة

عند طلب ميزة جديدة:

1. اشرح الحالة الاستخدام
2. قدم أمثلة إن أمكن
3. ناقش التأثير على الأداء

## قواعس السلوك

- كن محترماً مع الآخرين
- تقبل النقد البناء
- ركز على الكود وليس الشخص

شكراً لمساهمتك! 🎉
